# My Blog using [Pelican](http://docs.getpelican.com/)

## Setup


    git clone <url-of this repo>
    cd <cloned-repo>

    # activate virtualenv
    virtualenv venv && source venv/bin/activate

    # install the depenciences
    pip install -r requirements.txt
